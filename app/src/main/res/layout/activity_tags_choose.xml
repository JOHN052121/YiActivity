<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        xmlns:zhy="http://schemas.android.com/tools"
        android:fitsSystemWindows="true"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_height="match_parent">
    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:id="@+id/tags_choose_topLayout">
        <ImageView
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:src="@drawable/back_icon"
                android:id="@+id/tags_choose_back"
                android:layout_marginTop="13dp"
                android:layout_marginStart="10dp">
        </ImageView>

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="活动标签"
                android:layout_centerInParent="true"
                android:textSize="19sp"
                android:textColor="#000000">
        </TextView>

    </RelativeLayout>

    <com.zhy.view.flowlayout.TagFlowLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/tags_flowlayout"
            android:padding="20dp"
            android:layout_below="@+id/tags_choose_topLayout"
            zhy:max_select="-1">
    </com.zhy.view.flowlayout.TagFlowLayout>

    <Button
            android:layout_width="160dp"
            android:layout_height="45dp"
            android:text="保存"
            android:id="@+id/save_choose_tags"
            android:textColor="#ffffff"
            android:textSize="16sp"
            android:layout_alignParentBottom="true"
            android:layout_marginTop="30dp"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="50dp"
            android:background="@drawable/detail_activity_enroll_button">
    </Button>

</RelativeLayout>